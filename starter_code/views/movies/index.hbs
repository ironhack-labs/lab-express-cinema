    <h1>Ironhack Cinema</h1>
    <p>Click on the movie to check the showtimes!</p>

<section class="row">

    {{#each movies as |m|}}
        <article class="col-4 text-center mb-4">
            <img src="{{m.image}}" alt=<"{{m.title}}">
            <p class="pt-2">{{m.title}}</p>
            <a href="movies/{{m.id}}">See more</a>
        </article>
    {{/each}}

</section>