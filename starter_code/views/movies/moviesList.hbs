    <div class="container">
    <h1>MOVIES LIST</h1>
    <div class="row">
        {{#each movies}}
        
        <div class="col-md-3">
            <img src="{{image}}" alt="">
            <h6>{{title}}</h6>
            <a href="/movies/{{_id}}">See more</a>
        </div>

        {{/each}}
    </div>
</div>