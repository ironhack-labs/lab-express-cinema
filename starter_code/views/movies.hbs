<h1>Ironhack Cinema</h1>
<h2>Click on the movies to check showtimes!</h2>
<div class="grid">
    {{#each foundMovies}}
    <div class="movie"> 
        <img src="{{this.image}}" alt="">
        <div class="info">
            <p class="title">{{this.title}}</p>
            <a href="/movie/{{this._id}}">See more</a>
        </div>
    </div>
    {{/each}}
</div>
