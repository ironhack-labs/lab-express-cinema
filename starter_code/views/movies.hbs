<div class="container">
    {{#each movies}}
    <div class="one-item">
        <img src="{{image}}" alt="image">
        <p>{{title}}</p>
        <a href="/movie/{{_id}}">See more</a>
    </div>
    {{/each}}
</div>