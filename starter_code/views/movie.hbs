<section>
    <article class="this-movie">
        <img src="{{movie.image}}" alt="{{movie.title}} Mini">
        <h2>{{movie.title}}</h2>
        <h3>Directed by {{movie.director}}</h3>
        <strong>Staring :</strong> {{movie.stars}}
        <p>{{movie.description}}</p>
        {{movie.showtimes}}
    </article>
</section>