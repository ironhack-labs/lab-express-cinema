<div>
    <img src="{{image}}" alt="MOVIE">

    <div>
        <a href="/movies">Go back</a>
        {{title}}
        {{director}}
        <ul>
            {{#each stars}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
        {{description}}
        <p>
            {{#each showtimes}}
            {{this}} 
            {{/each}}
        </p>
    </div>
</div> 