
            <img src="{{image}}" alt="{{title}} cover">

            <br>
            <a href="/movies">Go back</a>
            <h2>{{title}}</h2>
            <h4>Director: {{director}}</h4>
            <h6>Stars:</h6>
            <ul>
                {{#each stars}}
                    <li>{{this}}</li>
                {{/each}}
            </ul>
            <p>{{description}}</p>
            {{#each showtimes}}
                {{this}}
            {{/each}}
