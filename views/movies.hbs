<div class="container">

    {{#each movies}}
    <div class="movie-container">
        <img src="{{this.image}}" alt="">
        <h1>{{this.title}}</h1>


        <a href="/{{this._id}}">See more</a>
    </div>
    {{/each}}

</div>