<!DOCTYPE html>
<html>
  <head>
    <title>Movie App - Movies</title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
  </head>
  <body>
    <div class="container">
      <h1>Movies</h1>
      <ul class="movie-list">
        {{#each movies}}
        <li>
          <img src="{{this.image}}" alt="{{this.title}}" class="movie-image">
          <h2>{{this.title}}</h2>
          <a href="/movies/{{id}}" class="see-more">See More</a>
        </li>
        {{/each}}
      </ul>
    </div>
  </body>
</html>
