<h1>Ironhack Cinema</h1>
<br>
<h2>Click on the movie to check the showtimes</h2>

{{#each movies}}
<div class="multi-movies">
    <br>
    <img src="{{this.image}}" alt="image" />
    <p>{{this.title}}</p>
    <a href="movie/{{this._id}}" class="href">See More</a>
    <br>
</div>

{{/each}}