<h1>movies</h1>
<div>

      {{#each movie}}
      <div class="movie-card">
          <img src="{{this.image}}" alt="Image Movie">
          <h3>{{this.title}}</h3>
          <a href="/movies/{{_id}}">See more</a>
      </div>
      {{/each}}

  </div>
