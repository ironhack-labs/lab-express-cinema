

<div class="row movies">
    {{#each films }}

    <div class="col-md-3 moviesCol">
    <img src="{{image}}" alt="{{title}}+image">
    <p>{{title}}</p>
    <a href="movies/{{id}}">detalles</a>
    </div>

    {{/each }}
</div>