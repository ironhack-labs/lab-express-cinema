<ul>
    {{#each movies}}
    <li>
        <img src="{{this.image}}" alt="Image of {{this.title}}">
        <h2>{{this.title}}</h2>
        <a href="/movies/{{this.id}}">See more</a>
    </li>

    {{/each}}
</ul>