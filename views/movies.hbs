<h2>Hola MOVIES</h2>

{{#if movies}}
    {{#each movies}}
        <div class="result-box">
        <img src="{{this.image}}" alt="{{this.title}}" />
        <h2>{{this.title}}</h2>
        <a href="/movies/{{this.id}}/">See more</a>
        </div>
    {{/each}}
{{/if}}