<div class="container">
    <div class="row">
        {{#each movies}}
        <div class="col-md-4 mb-4">
            <div class="info">
                <img class="imagen" src="{{image}}" alt="moviesimage">
                <h2>{{title}}</h2>
                <a href="/movies/{{_id}}" class="btn btn-dark">Detalles</a>
            </div>
        </div>
        {{/each}}
    </div>
    <hr>
    <a href="/" class="btn btn-dark">Home</a>
</div>