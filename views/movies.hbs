<section class="container">
    {{#each movies}}
        <div class="movies">
        <img src="{{this.image}}" alt="{{this.title}}">
        <h3>{{this.title}}</h3>
        <button id="buttonSeeMore"><a href="/movies/{{ this._id }}">See more</a></button>
        </div>
    {{/each}}
</section>