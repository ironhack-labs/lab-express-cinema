<div>
  <h1>Ironhack Cinema</h1>
  <p>Click on the movie to check the showtimes!</p>
</div>

<section>
  {{#each allmovies}}
    <div width="15%">

      <img src="{{image}}" alt="" />

      <h5>{{title}}</h5>
      <a href="/movies/{{_id}}">See more</a>
    </div>
  {{/each}}
</section>