<div class="movies-container">
    {{#each moviesfromDB}}
    <div class="movie-card">
        <img src="{{this.image}}" alt="{{this.title}}">
        <h3>{{this.title}}</h3>
        <a class="see-more-link" href="/movies/{{this._id}}">See more</a>
    </div>
    {{/each}}

</div>