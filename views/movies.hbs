

<container>

   
    {{#each movies}}

    <p>{{title}}</p>
    <img src={{image}} />
    <a href='/movie/{{_id}}' type="button" class="btn btn-dark">
        See more
    </a>
    {{/each}}
<container>

    <a href='/' type='button' class='btn btn-dark'>Back to the home page</a>