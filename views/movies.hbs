<h1>{{title}}</h1>
{{#each moviesDB}}
<div class="movieCard">
    <h4>{{title}}</h4>
    <img src="{{image}}" alt="{{title}} poster">
    <button class="detailsBtn"><a href="/movie/{{_id}}">See more</a></button>
</div>
{{/each}}