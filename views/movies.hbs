<div class="container">
    {{#each movies}}
    <div class="">
        <img src="{{image}}" alt="{{title}}">
        <h3>{{title}}</h3>
        <a href="/movies/{{_id}}">See more</a>
    </div>
    {{/each}}
</div>