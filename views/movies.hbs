<h1>All movies</h1>
<ul id="movies">
  {{#each movies}}
    <div class="eachMovie">
      <img src="{{image}}" alt="{{title}}" /><br />

      <li class="movie-title"><strong>{{title}}</strong></li>
      <a href="/movies/{{_id}}">See more</a>
    </div>
  {{/each}}

</ul>