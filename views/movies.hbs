<div class="movies-page">
  {{#each moviesArray}}

    <div class="card">
      <img src="{{image}}" alt={{title}} class="image" />
      <h3>{{title}}</h3>
      <a href="/movies/{{_id}}">See more</a>
    </div>

  {{/each}}
</div>