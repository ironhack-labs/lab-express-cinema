<h1>all movies</h1>
<ul>
    {{#each list}}
    <li class="{{#if this.isRegistered}}is-registered{{/if}}">
        <h2>{{this.title}}</h2>
        <img src={{this.image}}"" alt="Affiche">
        <a href="/movies/{{this._id}}">See more</a>
    </li>
    {{/each}}
</ul>