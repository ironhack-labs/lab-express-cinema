<div class="container">
    <div class="row">
        {{#each movies}}
        <div class="col-sm-4">
            <div class="card">
                <img src={{image}} class="card-img-top" alt="movie image">
                <p>{{title}}</p>
                <div class="card-body">
                    <a href="/pelis/detalles/{{_id}}">See more</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

</div>

<a href="/" class="btn btn-dark">Volver a incio</a>