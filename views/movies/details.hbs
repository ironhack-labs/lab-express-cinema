

{{#each movieList}}
    <a href="/movies">Go back</a>
    <div>
    <img src="{{image}}" width="{{image}}" heigth="{{image}}" alt="">
    </div>
    <div>
        <h2>{{title}}</h2>
        <h3>{{director}}</h3>
        <ul>{{stars}}</ul>
        <div>{{description}}</div><br>
        <div>{{showtimes}}</div>
         
    </div>
    <br><br>

{{/each}}
