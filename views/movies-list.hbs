<div class='container movies-container'>
    {{#each movies}}
        <div class='movie'>
            <img src="{{image}}" alt="">
            <h3>{{title}}</h3>
            <a href="/movies/{{_id}}">See more</a>
        </div>
    {{/each}}
</div>

