<section class="detail-page">
        <div>
                <img src="{{image}}" alt="">
        </div>

        <div>
                <a href="/movies">Go back</a>
                <h1>{{title}}</h1>
                <h2>Director: {{director}}</h2>
                <h3>Stars</h3>
                <ul>
                        {{#each stars}}
                        <li>{{this}}</li>
                        {{/each}}
                </ul>
                <p>{{description}}</p>
                <ul>
                        {{#each showtimes}}
                        <li>{{this}} | </li>
                        {{/each}}
                </ul>
        </div>

</section>