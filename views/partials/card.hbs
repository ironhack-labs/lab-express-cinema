<div class="card">
  <img src="{{movie.image}}" class="card-img-top" alt="{{movie.title}}" />
  <div class="card-body">
    <h5 class="card-title">{{movie.title}}</h5>
    <a class="btn btn-danger" href="/movies/{{movie._id}}">See details</a>
  </div>
</div>