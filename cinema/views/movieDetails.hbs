<img src="{{movie.image}}" alt="">

<h2>{{movie.title}}</h2>
<h2>{{movie.director}}</h2>

<ul>stars
  {{#each movie.stars}}
  <li>{{this}}</li>
  
  {{/each}}
  </ul>
<p>{{movie.description}}</p>
<span>
  {{#each movie.showtimes}}
  {{this}} | 
  
  {{/each}}
  </span>
  
 